CREATE TABLE dish(
  "id" TEXT,
  "name" TEXT,
  "menus_appeared" INTEGER,
  "times_appeared" INTEGER,
  "first_appeared" INTEGER,
  "last_appeared" INTEGER,
  "lowest_price" REAL,
  "highest_price" REAL
);